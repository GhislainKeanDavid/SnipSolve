# SnipSolve Environment Configuration
# All AI processing uses local LLM (Gemma 2 2B) - no API keys needed!
# The model will be auto-downloaded to ~/.snipsolve/models/ on first launch

# Optional: Uncomment if you want to use OpenAI as fallback (not recommended)
# OPENAI_API_KEY=your_openai_api_key_here
